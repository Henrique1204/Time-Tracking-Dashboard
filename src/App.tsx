import React from 'react';

import SideBar from './Componentes/SideBar';

const App = (): JSX.Element => {
  return (
    <main className="App">
      <SideBar />
    </main>
  );
};

export default App;
